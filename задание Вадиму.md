
## 🛠 Задание: «Глобальный Центр Уведомлений»

### Суть задачи

Тебе нужно создать контекст, который управляет списком уведомлений. Любой компонент в приложении должен иметь возможность добавить новое сообщение в этот список.

### Что нужно сделать:

1. **`NotificationContext`**: Создай контекст, который хранит массив объектов-уведомлений.
    
    - Структура объекта: `{ id: number, text: string, type: 'success' | 'error' }`.
        
2. **`NotificationProvider`**: Компонент-обертка, который содержит логику:
    
    - Состояние (массив уведомлений).
        
    - Функцию `addNotification(text, type)`, которая добавляет новое сообщение.
        
    - (Опционально для продвинутых) Функцию `removeNotification(id)`, которая удаляет сообщение.
        
3. **`NotificationList`**: Компонент, который подписывается на контекст и просто отрисовывает список всех текущих уведомлений (например, в правом верхнем углу экрана).
    
4. **`ActionButtons`**: Отдельный компонент с парой кнопок («Показать успех», «Показать ошибку»), которые вызывают `addNotification` через контекст.
    

---

### Стартовый шаблон 



```jsx
// Пример того, как это может выглядеть в использовании
function App() {
  return (
    <NotificationProvider>
      <div className="app-container">
        <h1>Мое крутое приложение</h1>
        <ActionButtons />
        <NotificationList />
      </div>
    </NotificationProvider>
  );
}
```


#### Чтобы было более понятно насчёт ActionButtons

```jsx
const ActionButtons = () => {
  // Достаем функцию из нашего созданного контекста
  const { addNotification } = useContext(NotificationContext);

  return (
    <div style={{ display: 'flex', gap: '10px', marginTop: '20px' }}>
      {/* При нажатии вызываем функцию из контекста */}
      <button onClick={() => addNotification('Данные успешно сохранены!', 'success')}>
        ✅ Показать успех
      </button>
      
      <button onClick={() => addNotification('Ошибка при загрузке', 'error')}>
        ❌ Показать ошибку
      </button>
    </div>
  );
};
```