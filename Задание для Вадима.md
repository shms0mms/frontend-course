
## 🎯 Идея

Приложение должно позволять:

- просматривать список проектов
    
- переходить на страницы отдельных проектов
    
- работать с задачами внутри проектов
    
- использовать вложенные маршруты, динамические параметры и защищённые маршруты
    

---

## 🗺 Структура страниц (Routes)

Приложение должно иметь следующие маршруты:

```
/                → Главная
/login           → Страница входа
/dashboard       → Панель управления (доступна только после входа)
/projects        → Список проектов
/projects/:id    → Страница конкретного проекта
/projects/:id/tasks → Задачи проекта
/projects/:id/settings → Настройки проекта
/profile         → Профиль пользователя
/*               → Страница 404
```

---

## 🔐 Авторизация (логика)

Реализуй простую fake-авторизацию:

- пользователь «входит» через `/login`
    
- факт входа хранится в `localStorage`
    
- если пользователь не авторизован — доступ к `/dashboard`, `/projects`, `/profile` запрещён
    
- неавторизованный пользователь должен автоматически перенаправляться на `/login`
    

---

## 🧭 Навигация

Сделай навбар, который:

- отображается на всех страницах кроме `/login`
    
- содержит ссылки:
    
    - Dashboard
        
    - Projects
        
    - Profile
        
    - Logout
        
- подсвечивает активный маршрут
    

---

## 📁 Проекты

На странице `/projects`:

- отображается список проектов
    
- каждый проект — ссылка на `/projects/:id`
    
- данные хранятся в виде массива объектов в state
    

Пример:

```js
[
  { id: "1", name: "Online Shop" },
  { id: "2", name: "Blog Platform" },
  { id: "3", name: "CRM System" }
]
```

---

## 📄 Страница проекта `/projects/:id`

На этой странице:

- показывается название проекта
    
- есть внутреннее меню:
    
    - Overview
        
    - Tasks
        
    - Settings
        

Эти пункты должны быть реализованы через **nested routes**

---

## 📋 Tasks `/projects/:id/tasks`

В этом разделе:

- список задач проекта
    
- возможность добавить новую задачу
    
- возможность удалить задачу
    
- задачи хранятся в состоянии проекта
    

---

## ⚙ Settings `/projects/:id/settings`

Здесь можно:

- изменить название проекта
    
- сохранить изменения в общем состоянии
    

---

## 👤 Profile `/profile`

Страница должна показывать:

- имя пользователя
    
- email
    
- кнопку «Выйти», которая:
    
    - очищает localStorage
        
    - перенаправляет на `/login`
        

---

## 🚫 Страница 404

Если пользователь перешёл по несуществующему пути — отображается:

```
Страница не найдена
[Вернуться на главную]
```

---

## ⭐ что тебе можно юзать

Используй:

- `useParams`
    
- `useNavigate`
    
- `Outlet`
    
- `Link`
    
- `useEffect` для проверки авторизации

