
## 📝 Задание: Приложение "Smart Task Dashboard"

### Суть проекта

Нужно создать приложение, где пользователь управляет списком задач, видит статистику и может быстро переключаться между темами оформления.

### Технические требования по пунктам:

#### 1. Context API (`useContext`)

- Создай `ThemeContext`, который хранит текущую тему (light/dark).
    
- Оберни все приложение в провайдер.
    
- Добавь переключатель темы, который меняет CSS-переменные или классы всего контейнера.
    

#### 2. Состояние (`useState`)

- Реализуй список задач. Каждая задача — это объект `{ id, text, completed }`.
    
- Должна быть возможность добавлять новые задачи и отмечать их как выполненные.
    

#### 3. Оптимизация (`useMemo`)

- Добавь блок «Аналитика». В нем должны вычисляться:
    
    1. Общее количество задач.
        
    2. Количество только выполненных задач.
        
- **Важно:** Эти расчеты должны быть обернуты в `useMemo`, чтобы они не пересчитывались при простой смене темы оформления (через контекст).
    

#### 4. Прямой доступ к DOM и хранение данных (`useRef`)

- **Фокус:** При нажатии на кнопку «Добавить задачу» фокус должен автоматически возвращаться в поле ввода (input) с помощью `useRef`.
    
- **Секундомер:** Реализуй таймер «Времени на странице» (просто секунды). Используй `useRef` для хранения ID интервала (`setInterval`), чтобы корректно очистить его при размонтировании компонента, не вызывая лишних ререндеров.
    
