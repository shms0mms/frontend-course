
## 🔹 Что такое кастомный хук?

**Кастомный хук** — это обычная JavaScript-функция,  
которая использует встроенные хуки React и помогает выносить логику в отдельный модуль.

### 📌 Главное правило:
Имя хука **обязательно начинается с `use`**

Пример:
```js
useAuth
useFetch
useForm
useTheme
```

## 🔹 Зачем нужны кастомные хуки?

Кастомные хуки позволяют:

✅ Переиспользовать логику  
✅ Делать код чище  
✅ Уменьшать дублирование  
✅ Упрощать компоненты

### ❌ Без кастомного хука:

```js
useEffect(() => {
  fetchData();
}, []);
```

повторяется в каждом компоненте

### ✅ С кастомным хуком:

```js
const { data } = useFetch(url);
```

---

## 🔹 Структура кастомного хука

Пример базового шаблона:

```js
function useSomething() {
  // хуки
  const [state, setState] = useState();

  // логика
  function doSomething() {}

  // возврат
  return { state, doSomething };
}
```

---

## 🔹 Пример 1: useCounter

### 📌 Хук:

```js
import { useState } from "react";

export function useCounter(initial = 0) {
  const [count, setCount] = useState(initial);

  const increment = () => setCount(c => c + 1);
  const decrement = () => setCount(c => c - 1);
  const reset = () => setCount(initial);

  return {
    count,
    increment,
    decrement,
    reset
  };
}
```

---

### 📌 Использование:

```js
const { count, increment, decrement } = useCounter(10);
```

---

## 🔹 Пример 2: useFetch (работа с API)

### 📌 Хук:

```js
import { useState, useEffect } from "react";

export function useFetch(url) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    async function fetchData() {
      try {
        setLoading(true);

        const res = await fetch(url);
        const json = await res.json();

        setData(json);
      } catch (err) {
        setError(err.message);
      } finally {
        setLoading(false);
      }
    }

    fetchData();
  }, [url]);

  return { data, loading, error };
}
```

---

### 📌 Использование:

```js
const { data, loading, error } = useFetch("/api/users");
```

---

## 🔹 Пример 3: useLocalStorage

### 📌 Хук:

```js
import { useState, useEffect } from "react";

export function useLocalStorage(key, initialValue) {
  const [value, setValue] = useState(() => {
    const saved = localStorage.getItem(key);
    return saved ? JSON.parse(saved) : initialValue;
  });

  useEffect(() => {
    localStorage.setItem(key, JSON.stringify(value));
  }, [key, value]);

  return [value, setValue];
}
```

---

### 📌 Использование:

```js
const [theme, setTheme] = useLocalStorage("theme", "light");
```

---

## 🔹 Правила использования хуков

### ⚠️ Кастомные хуки подчиняются тем же правилам что и обычные:

✅ Вызывать только:

- в компонентах
    
- в других хуках
    

❌ Нельзя:

- в if
    
- в циклах
    
- в функциях
    

---

## 🔹 Что можно возвращать из хука?

### 1️⃣ Объект

```js
return { data, loading };
```

### 2️⃣ Массив (как useState)

```js
return [value, setValue];
```

Выбор зависит от логики.

---

## 🔹 Когда создавать кастомный хук?

Создавай хук, если:

✅ Код повторяется  
✅ Логика сложная  
✅ Компонент стал большим  
✅ Нужно переиспользование

---

## 🔹 Связь с useContext

Часто хук работает вместе с Context:

```js
function useAuth() {
  return useContext(AuthContext);
}
```

Упрощает доступ к глобальному состоянию.

---

## 🔹 Типичные ошибки

❌ Не использовать `use` в названии  
❌ Хранить JSX в хуке  
❌ Делать слишком большой хук  
❌ Смешивать разные задачи в одном хуке

---

## 🔹 Практические задания

### 📝 Задание 1

Создай `useToggle`

Функции:

- toggle()
    
- true / false
    

---

### 📝 Задание 2

Создай `useInput`

Для input:

- value
    
- onChange
    
- reset
    

---

### 📝 Задание 3

Создай `useTimer`

Функции:

- start
    
- stop
    
- reset
    
