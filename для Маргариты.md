
## Задание: «Интерактивные карточки»

Твоя задача — создать три одинаковых квадрата и применить к каждому свой эффект при наведении курсора (`:hover`).

### 1. Подготовка (HTML)

Создай три блока с общим классом и уникальными ID:

HTML

```
<div class="container">
  <div class="box" id="rotate">Rotate</div>
  <div class="box" id="scale">Scale</div>
  <div class="box" id="move">Translate</div>
</div>
```

### 2. Стилизация (CSS)

Добавь базовые стили, чтобы блоки выглядели аккуратно:

CSS

```
.container {
  display: flex;
  gap: 20px;
  padding: 50px;
}

.box {
  width: 100px;
  height: 100px;
  background-color: #3498db;
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: sans-serif;
  transition: transform 0.3s ease; /* Чтобы анимация была плавной */
  cursor: pointer;
}
```

### 3. Сами трансформации (Твоя работа)

Допиши CSS для каждого блока, используя следующие правила:

1. **Поворот:** Для `#rotate` при наведении сделай поворот на 45 градусов.
    
    - Подсказка: `transform: rotate(45deg);`
        
2. **Увеличение:** Для `#scale` при наведении увеличий блок в 1.2 раза.
    
    - Подсказка: `transform: scale(1.2);`
        
3. **Смещение:** Для `#move` при наведении сдвинь блок на 20 пикселей вверх.
    
    - Подсказка: `transform: translateY(-20px);`
        

---

### 💡 бонусный, если успеешь

Попробуй совместить несколько трансформаций в одной строке для нового блока. Например:

`transform: rotate(10deg) scale(1.1) translateX(10px);`

**Что это даст:** Ты увидишь, что порядок функций имеет значение! Сначала выполняется то, что написано первым.